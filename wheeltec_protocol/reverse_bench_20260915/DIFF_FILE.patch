--- a/reverse_bench.py
+++ b/reverse_bench.py
@@ -9,8 +9,8 @@
 
 from driver_reference import FrameParser, build_frame, STOP_FRAME, DEFAULT_PORT
 
-PULSE_SPEED = 0.0
-PULSE_SECONDS = 1.0
+PULSE_SPEED = -0.05
+PULSE_SECONDS = 3.0
 STARTUP_STOP_SECONDS = 3.0
 FINAL_STOP_SECONDS = 10.0
 
@@ -109,11 +109,11 @@
 def main():
     if sys.argv[1:]!=['--execute-one-shot']:
         raise SystemExit('Explicit --execute-one-shot required')
-    if PULSE_SPEED not in (0.0,-.05) or PULSE_SECONDS!=1.0:raise SystemExit('Unexpected commissioning parameters')
+    if PULSE_SPEED not in (0.0,-.05) or PULSE_SECONDS!=3.0:raise SystemExit('Unexpected commissioning parameters')
     import serial
     p=Path(__file__).resolve().parent
     # An attempted execution consumes this marker; service restart cannot repeat a pulse.
-    fd=os.open(p/'attempt.started',os.O_WRONLY|os.O_CREAT|os.O_EXCL,0o600);os.close(fd)
+    fd=os.open(p/'attempt-3s.started',os.O_WRONLY|os.O_CREAT|os.O_EXCL,0o600);os.close(fd)
     ser=serial.Serial(DEFAULT_PORT,115200,timeout=0,write_timeout=.02,exclusive=True)
     bench=Bench(ser)
     signal.signal(signal.SIGTERM,lambda *_:setattr(bench,'interrupted',True))
