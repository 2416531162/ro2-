# ROS 2 rclpy/messages and PyQt5 are installed through the board's ROS/OS packages.
numpy>=1.24,<2
opencv-python>=4.8,<5
pyserial>=3.5,<4
# RK3588 only: install rknn-toolkit-lite2 separately and match it to the
# board's librknnrt/driver. Jetson is also aarch64 Linux, so a platform marker
# cannot safely distinguish the two boards here.
